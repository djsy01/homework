{
  "scripts": {
    "start": "live-server . --port=4000 --host=localhost --no-browser",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "live-server": "^1.2.0",
    "prettier": "^3.7.4"
  }
}
